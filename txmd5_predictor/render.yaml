services:
  - type: worker
    name: txmd5-predictor-worker
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
