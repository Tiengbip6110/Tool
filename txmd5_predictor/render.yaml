services:
  - type: worker
    name: txmd5-predictor
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    rootDir: txmd5_predictor
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
